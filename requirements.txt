# MKIV Academic Intelligence Graph Engine — Dependencies
# ================================================================
# 核心依赖：CLI 流水线 + 桌面 GUI 开箱即用。
# NLP 语义聚类 (Step 3) 需要额外的 sentence-transformers / scikit-learn，
# 请另行安装: pip install -r requirements-ml.txt

# === Core Pipeline ===
pyyaml>=6.0
requests>=2.31.0
pandas>=2.0.0
openpyxl>=3.1.0
tqdm>=4.65.0
urllib3>=2.0.0

# === Chinese Name Processing ===
pypinyin>=0.49.0

# === Graph Database ===
neo4j>=5.14.0

# === Web Server ===
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# === GUI (Desktop Application) ===
PySide6>=6.6.0
# Note: QtWebEngine is bundled within PySide6

# === Packaging (dev only) ===
# pyinstaller>=6.0
